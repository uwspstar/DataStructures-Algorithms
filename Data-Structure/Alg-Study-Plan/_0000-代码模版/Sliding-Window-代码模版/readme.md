```js
for (快指针 in 可迭代集合) {
  更新窗口内信息;
  while (窗口内不符合题意) {
    扩展或者收缩窗口;
    慢指针移动;
  }
  更新答案;
}
```

```js
let j =0
for (int i = 0; i < n; i++) {
// 不满足则循环到满足搭配为止
    while (j < n && i 到 j 之间不满足条件) {
        j += 1;
    }
    if (i 到 j 之间满足条件) {
        处理 i，j 这次搭配
    }
}
```
